# git-cliff configuration
# https://git-cliff.org/docs/configuration

[changelog]
header = """
# Changelog

"""

body = """
{% if version %}
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}
## [unreleased]
{% endif %}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim }}

{% for commit in commits -%}
- {% if commit.scope %}*({{ commit.scope }})* {% endif %}{% if commit.breaking %}**BREAKING:** {% endif %}{{ commit.message | upper_first }}
{% endfor %}

{% endfor %}
"""

footer = """
<!-- generated by git-cliff -->
"""

trim = true

postprocessors = []

[git]

conventional_commits = true
filter_unconventional = true
split_commits = false

commit_preprocessors = []

commit_parsers = [

  # Features
  { message = "^feat", group = "<!-- 0 -->🚀 Features" },

  # Bug fixes
  { message = "^fix", group = "<!-- 1 -->🐛 Bug Fixes" },

  # Refactoring and typing improvements
  { message = "^refactor", group = "<!-- 2 -->♻️ Refactoring" },
  { message = "^chore\\(mypy\\)", group = "<!-- 2 -->♻️ Refactoring" },
  { message = "^fix\\(mypy\\)", group = "<!-- 2 -->♻️ Refactoring" },
  { message = "^refactor\\(typing\\)", group = "<!-- 2 -->♻️ Refactoring" },

  # Documentation
  { message = "^docs?", group = "<!-- 3 -->📚 Documentation" },

  # Performance
  { message = "^perf", group = "<!-- 4 -->⚡ Performance" },

  # Formatting and style
  { message = "^style", group = "<!-- 5 -->🎨 Styling" },

  # Testing
  { message = "^test", group = "<!-- 6 -->🧪 Testing" },

  # Build system
  { message = "^build", group = "<!-- 7 -->⚙️ Build System" },

  # Ignore release commits
  { message = "^chore\\(release\\)", skip = true },
  { message = "^docs\\(release\\)", skip = true },

  # CI/CD
  { message = "^ci", group = "<!-- 8 -->🏗️ CI/CD" },
  { message = "^chore\\((ci)\\)", group = "<!-- 8 -->🏗️ CI/CD" },

  # Dependencies
  { message = "^chore\\(deps", group = "<!-- 9 -->📦 Dependencies" },

  # Maintenance
  { message = "^chore", group = "<!-- 10 -->⚙️ Maintenance" },

  # Security
  { body = "(?i).*security.*", group = "<!-- 11 -->🛡️ Security" },

  # Reverts
  { message = "^revert", group = "<!-- 12 -->◀️ Revert" },

  # Ignore changelog maintenance
  { message = "^docs\\(changelog\\)", skip = true },
  { message = "^chore\\(cliff\\)", skip = true },

  # Ignore lockfile-only commits
  { message = "^chore.*lock", skip = true },

  # Ignore automated PR maintenance
  { message = "^chore\\(pr\\)", skip = true },
  { message = "^chore\\(pull\\)", skip = true },
]

filter_commits = false

topo_order = false

sort_commits = "oldest"